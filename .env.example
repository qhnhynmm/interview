# Copy to .env at repo root for docker compose.
# Used by docker-compose.yml — do not commit real secrets.

POSTGRES_USER=aurelia
POSTGRES_PASSWORD=aurelia
POSTGRES_DB=aurelia
POSTGRES_PORT=5432

BACKEND_PORT=8000
JWT_SECRET=change-me-to-a-long-random-string