# =============================================================================
# Aurelia — Docker Compose (.env ở thư mục gốc repo)
# Copy:  cp .env.example .env
# Chạy:  docker compose up -d --build
# =============================================================================

# --- PostgreSQL ---
POSTGRES_USER=aurelia
POSTGRES_PASSWORD=aurelia
POSTGRES_DB=aurelia
POSTGRES_PORT=5432

# --- Backend API ---
BACKEND_PORT=8000
JWT_SECRET=change-me-to-a-long-random-string

# URL frontend (dùng tạo meeting link trong email/UI)
FRONTEND_URL=http://localhost:5173

# --- MinIO (lưu CV PDF, recordings, reports) ---
# Console: http://localhost:9001
MINIO_ROOT_USER=aurelia
MINIO_ROOT_PASSWORD=aurelia-secret
MINIO_ENABLED=true
MINIO_ENDPOINT=127.0.0.1:9000
MINIO_ACCESS_KEY=aurelia
MINIO_SECRET_KEY=aurelia-secret

# --- CV extraction — Google Gemini (BẮT BUỘC để parse PDF/DOCX) ---
# Lấy key tại: https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Model mặc định: gemini-2.0-flash (có thể đổi sang gemini-1.5-flash, gemini-2.5-flash, ...)
GEMINI_MODEL=gemini-2.0-flash

# --- AI Services (Planning Agent — tùy chọn, fallback mock nếu không chạy) ---
AI_SERVICE_URL=http://localhost:8001