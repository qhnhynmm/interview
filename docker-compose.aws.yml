# AWS EC2 overlay — dùng cùng docker-compose.yml:
#   ./scripts/generate-livekit-config.sh configs/livekit.aws.yaml.template configs/livekit.aws.yaml
#   docker compose -f docker-compose.yml -f docker-compose.aws.yml up -d --build
#
# LiveKit cần use_external_ip trên cloud + mở UDP 50000-50100 trong Security Group.

services:
  livekit-config:
    environment:
      LIVEKIT_TEMPLATE: configs/livekit.aws.yaml.template
      LIVEKIT_OUTPUT: configs/livekit.aws.yaml

  livekit:
    volumes:
      - ./configs/livekit.aws.yaml:/etc/livekit.yaml:ro